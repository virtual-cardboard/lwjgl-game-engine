package context.visuals.gui;

import context.GLContext;
import context.data.GameData;

public class InvisibleGui extends Gui {

	@Override
	public final void render(GLContext glContext, GameData data, float parentX, float parentY, float parentWidth, float parentHeight) {
	}

}